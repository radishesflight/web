# 生产环境配置
# npm run build 时会读取这个文件

# 生产环境:配置真实后端域名
# 如果前后端同域,这里写 /
# 如果跨域,这里写完整 URL 例如 https://api.example.com
VITE_API_BASE=/api

# 生产环境不走 vite proxy,所以 VITE_API_TARGET 无效
# VITE_API_TARGET=https://api.example.com
